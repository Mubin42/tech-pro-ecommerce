import { Flex } from '@chakra-ui/react';
import React, { FC } from 'react';

type CenterSectionProps = {
	children: React.ReactNode;
};

const CenterSection: FC<CenterSectionProps> = ({ children }) => {
	// api

	// hooks

	// states

	// variables

	// styles

	// functions

	// effects

	// components

	return (
		<Flex maxW='1440px' flex={1} mx='auto'>
			{children}
		</Flex>
	);
};

export default CenterSection;
